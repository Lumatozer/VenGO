VenGO depends on wasmtime for the wasm runtime and fuel metering.

Please install wasmtime from https://wasmtime.dev/.

We also recommend you to install binaryen for better performance and smaller binary sizes.

Install wit-bindgen using `cargo install wit-bindgen-cli`.