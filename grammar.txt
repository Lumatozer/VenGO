.link
    grammar.vtl grammar
    v1.vtl      v1

.function _start (a bytes, b bytes)->(string)
    int a, b, c
    call (grammar._start, grammar.a, grammar.b) -> (a, b)
    add a, b -> c
    .if a {
        
    }
    return a, b