package main

function main() {
    var a->int;
    var i->int;
    for {
        if (i==100) {
            break;
        }
        db_read(1, "hi", a);
        a=a+1;
        db_write(1, "hi", a);
        i=i+1;
    }
}