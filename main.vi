package main

function main() {
    var a->int;
    var i->int;
    db_write(1, "hi", a);
    for {
        if (i==100) {
            break;
        }
        lock {
            db_read(1, "hi", a);
            a=a+1;
            db_write(1, "hi", a);
            i=i+1;
        }
    }
}